{
  "dependencies": {
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "build":"vite build",
    "dev": "vite",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  }
}
